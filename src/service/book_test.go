package service

/*
import (
	"testing"

	"github.com/Eli15x/SagwaBooks-Back/src/client"
	"github.com/Eli15x/SagwaBooks-Back/src/models"
	"github.com/stretchr/testify/assert"
	testifyMock "github.com/stretchr/testify/mock"
)

var (
	MongoDB *client.MongoDBMock
	Book    *BookMock
)

func Test_CreateBook_ShouldReturnOK(t *testing.T) {
	MongoDB = &client.MongoDBMock{}
	Book = &BookMock{}
	book := &models.Book{}
	Book.On("CreateNewBook", testifyMock.Anything, testifyMock.Anything, testifyMock.Anything, testifyMock.Anything, testifyMock.Anything, testifyMock.Anything).Return(nil)

	err := Book.CreateNewBook()

	assert.Equal(t, err, nil)
}*/
